spring:
  datasource:
    url: jdbc:h2:file:~/datadb;NON_KEYWORDS=VALUE
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: h2
    open-in-view: false
  security:
    user:
      name: admin
      password: password
